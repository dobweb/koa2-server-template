const Models{{ name }} = require('../models/Models{{ name }}')

// 初始化
const init = async function(ctx) {
  const data = ctx.request.method === 'POST' ? ctx.request.body : ctx.request.query
  ctx.body = await Models{{ name }}.CreateTable(data)
}

const Find{{ properCase name }} = async function(ctx) {
  const data = ctx.request.method === 'POST' ? ctx.request.body : ctx.request.query
  ctx.body = await Models{{ name }}.Find(data)
}

module.exports = {
  init,
  Find{{ properCase name }}
}